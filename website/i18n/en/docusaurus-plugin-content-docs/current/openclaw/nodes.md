# Nodes — iOS / Android Device Pairing

Pair mobile devices as lightweight OpenClaw nodes for remote control, notifications, and sensor access.

Use `openclaw node` commands to manage paired devices.
